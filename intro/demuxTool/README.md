# intro.demuxTool
Object-oriented, test-driven refactor of the standalone `demux.py` into a package.

- PyRTL-based N-way demultiplexer
- CSV/JSON I/O
- Optional VCD dump
- Import shim so `python cli.py` works from inside the package
- Class diagram (DOT/PNG) via `tools/class_diagram.py`
